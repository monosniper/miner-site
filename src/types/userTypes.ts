export type User = {
  id: number;
  name: string;
  options: object;
  token: string;
  username: string;
};

export type Balance = {
  usdt: number;
  btc: number;
  eth: number;
  doge: number;
  ton: number;
};
